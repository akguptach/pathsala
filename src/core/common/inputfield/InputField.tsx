import React from "react";
// import TextField from "@mui/material/TextField";

const InputField = () => {
  console.log("field, value, onChange, onBlur, error, helperText")
  return (
    <div className="form-group">
      <input
        // fullWidth
        // id={field.name}
        // label={field.label}
        // type={field.type}
        // name={field.name}
        // value={value}
        // onChange={onChange}
        // onBlur={onBlur}
        // placeholder={field.placeholder}
        // error={error}
        // helperText={helperText}
        // variant="outlined"
      />
    </div>
  );
};

export default InputField;
